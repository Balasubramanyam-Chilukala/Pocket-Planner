<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PocketPlanner</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body, html {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            height: 100%;
            overflow: hidden;
        }
        
        /* Animated Background */
        .background-animation {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(-45deg, #6b2b89, #8e44ad, #e74c3c, #ff7e5f);
            background-size: 400% 400%;
            animation: gradient 15s ease infinite;
            z-index: -2;
        }

        /* Floating Circles Animation */
        .floating-circles {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            overflow: hidden;
            z-index: -1;
        }

        .circle {
            position: absolute;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            animation: float 20s infinite;
        }

        .circle:nth-child(1) {
            width: 300px;
            height: 300px;
            left: -100px;
            top: -100px;
            animation-delay: 0s;
        }

        .circle:nth-child(2) {
            width: 400px;
            height: 400px;
            right: -150px;
            bottom: -150px;
            animation-delay: -5s;
        }

        .circle:nth-child(3) {
            width: 200px;
            height: 200px;
            right: 30%;
            top: 30%;
            animation-delay: -10s;
        }

        .navbar {
            background: linear-gradient(-45deg, #6b2b89, #8e44ad, #e74c3c, #ff7e5f); /* Match gradient */
            background-size: 400% 400%; /* Similar to the background animation */
            animation: gradient 15s ease infinite; /* Apply animation to the navbar */
            backdrop-filter: blur(10px); /* Optional blur effect */
            overflow: hidden;
            position: fixed;
            top: 0;
            width: 100%;
            height: 50px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 10px;
            z-index: 1000;
        }

        .navbar a {
            display: inline-block;
            color: white;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
            transition: all 0.3s;
        }

        .navbar a:hover {
            background-color: rgba(255, 255, 255, 0.2);
            transform: translateY(-2px);
        }

        .main-content {
            margin-top: 50px;
            height: calc(100% - 50px);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            position: relative;
        }

        .title {
            position: relative;
            text-align: center;
            color: white;
            margin-bottom: 20px;
            z-index: 1;
        }

        .title h1 {
            font-size: 3.5em;
            margin: 0;
            color: #ffffff;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.3);
            transition: all 0.3s ease;
        }

        .title h1:hover {
            font-size: 5em;
            text-shadow: 4px 4px 12px rgba(0, 0, 0, 0.4);
        }

        .title p {
            font-size: 1.3em;
            margin: 5px 0 0 0;
            color: #ffffff;
            font-style: italic;
        }

        .chatbot-icon {
            background-color: rgba(255, 255, 255, 0.2);
            color: white;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
            cursor: pointer;
            transition: all 0.3s;
            backdrop-filter: blur(5px);
        }

        .chatbot-icon:hover {
            transform: scale(1.1) rotate(15deg);
            background-color: rgba(255, 255, 255, 0.3);
        }

        .chatbot-icon.bottom {
            position: fixed;
            bottom: 20px;
            right: 20px;
            z-index: 1000;
        }

        @keyframes gradient {
            0% {
                background-position: 0% 50%;
            }
            50% {
                background-position: 100% 50%;
            }
            100% {
                background-position: 0% 50%;
            }
        }

        @keyframes float {
            0%, 100% {
                transform: translateY(0) translateX(0);
            }
            25% {
                transform: translateY(-50px) translateX(50px);
            }
            50% {
                transform: translateY(50px) translateX(-50px);
            }
            75% {
                transform: translateY(25px) translateX(25px);
            }
        }

        @media screen and (max-width: 600px) {
            .navbar a {
                padding: 10px 12px;
            }
            .title h1 {
                font-size: 2.5em;
            }
            .title h1:hover {
                font-size: 3em;
            }
        }
    </style>
</head>
<body>
    <div class="background-animation"></div>
    <div class="floating-circles">
        <div class="circle"></div>
        <div class="circle"></div>
        <div class="circle"></div>
    </div>

    <nav class="navbar">
        <div class="nav-links">
            <a href="/expenses">Expense Tracking</a>
            <a href="/subscriptions">Subscription</a>
            <a href="/invest">Investment</a>
            <a href="/financial-goals">Financial Goals</a>
            <!-- <a href="profile.html">Profile</a> -->
        </div>
        <div class="chatbot-icon">
            <a href="/finbot"><i class="fas fa-comment-dots"></i></a>
        </div>
    </nav>

    <main class="main-content">
        <div class="title">
            <h1>PocketPlanner</h1>
            <p>Your trusted financial management partner</p>
        </div>
    </main>

    <script src="https://cdn.botpress.cloud/webchat/v2.2/inject.js"></script>
<script src="https://files.bpcontent.cloud/2024/12/06/07/20241206074808-YQPVYIG8.js"></script>
</body>
</html>